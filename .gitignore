.DS_Store
node_modules
dist
build
coverage
.env
.env.local
*.log
*.tsbuildinfo
/tmp

# Claude Code: track shared agent scaffolding, ignore per-machine state.
.claude/settings.local.json
.claude/worktrees/
.claude/cache/
.claude/shell-snapshots/

# Visual QA output: we keep the capture harness + the JSON summary so the
# optimizer can read it next iteration; discard the PNGs (~megabytes per run).
visual-tests/*/*.png
