# Public site URL (used by sitemap generation and SEO canonical tags).
# infra/deploy.sh sets this to the production domain for you.
SITE_URL=https://ghulamsiddiq.com

# Where the booking-feedback fetch POSTs. There is currently no backend;
# only set this if a feedback service ever returns.
#VITE_API_BASE_URL=
